# CMPE341 Employee Lifecycle CSV Sync

This project is a Linux automation script that synchronizes user accounts
with HR employee records provided in CSV format.

The script supports onboarding, offboarding, reporting, and snapshot-based
change detection.
# cmpe341-employee-lifecycle-CSV-sync
